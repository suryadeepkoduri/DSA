# Union-Find  (Disjoint Set Union - DSU)

Union Find is a datastructure used to efficiently manage a collection of disjoint (non-overlapping) sets.

It supports two operations
- `find(x)` -> returns the representative (parent/root) of the set containing `x`.
- `union(x,y)` -> merges the sets containing `x` and `y`.